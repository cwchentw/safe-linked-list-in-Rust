# Safe Linked List in Rust

This repo demostrates linked list in Rust without using unsafe code.  However, due to the limiting feature of Rust compiler, some methods cannot be implemented effectively.
